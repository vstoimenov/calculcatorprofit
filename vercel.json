{
  "installCommand": "echo \"Static investor demo: skipping dependency install\"",
  "buildCommand": "mkdir -p dist && cp preview/standalone.html dist/index.html",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

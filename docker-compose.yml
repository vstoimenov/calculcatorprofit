services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: profitboard_bg
    ports:
      - "5432:5432"
    volumes:
      - profitboard_postgres:/var/lib/postgresql/data

volumes:
  profitboard_postgres:
